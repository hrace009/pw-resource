<%= form_for(@material_subtype) do |f| %>
  <% if @material_subtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material_subtype.errors.count, "error") %> prohibited this material_subtype from being saved:</h2>

      <ul>
      <% @material_subtype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
