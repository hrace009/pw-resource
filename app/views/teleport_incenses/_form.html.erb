<%= form_for(@teleport_incense) do |f| %>
  <% if @teleport_incense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teleport_incense.errors.count, "error") %> prohibited this teleport_incense from being saved:</h2>

      <ul>
      <% @teleport_incense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
