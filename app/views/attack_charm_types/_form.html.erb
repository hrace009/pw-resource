<%= form_for(@attack_charm_type) do |f| %>
  <% if @attack_charm_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attack_charm_type.errors.count, "error") %> prohibited this attack_charm_type from being saved:</h2>

      <ul>
      <% @attack_charm_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
