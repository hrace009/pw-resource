<%= form_for(@skilltome_type) do |f| %>
  <% if @skilltome_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skilltome_type.errors.count, "error") %> prohibited this skilltome_type from being saved:</h2>

      <ul>
      <% @skilltome_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
