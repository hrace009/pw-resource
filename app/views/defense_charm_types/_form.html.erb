<%= form_for(@defense_charm_type) do |f| %>
  <% if @defense_charm_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@defense_charm_type.errors.count, "error") %> prohibited this defense_charm_type from being saved:</h2>

      <ul>
      <% @defense_charm_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
